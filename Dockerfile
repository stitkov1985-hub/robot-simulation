# Используем легкую версию Python 2025 года
FROM python:3.12-slim

# Указываем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем файл вашей программы в контейнер
COPY Robot.py .

# Команда для запуска программы
# -u нужен, чтобы текст в консоли Docker появлялся мгновенно
CMD ["python", "-u", "Robot.py"]
